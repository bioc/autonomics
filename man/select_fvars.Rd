% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/24_fit.R
\name{annotationvars}
\alias{annotationvars}
\alias{contrastvars}
\alias{select_fvars}
\title{Select fvars}
\usage{
annotationvars(object)

contrastvars(
  object,
  fit = fits(object)[1],
  contrast = coefs(object, fit = fit)[1]
)

select_fvars(
  object,
  fit = fits(object)[1],
  contrast = coefs(object, fit = fit)[1],
  fvars = c(annotationvars(object), contrastvars(object, fit, contrast)),
  verbose = TRUE
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{verbose}{TRUE or FALSE}

\item{cols}{character vector}
}
\value{
SummarizedExperiment
}
\description{
Select fvars
}
\examples{
object <- survobj()
object \%<>\% fit_limma(~sex+age)
annotationvars(object)
  contrastvars(object)
}
